<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:UIPractice.MainApplication"
                    x:Class="UIPractice.MainApplication.Theme">
    
    <!-- Generic Theme -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="generic.xaml" />
    </ResourceDictionary.MergedDictionaries>
        
    <!-- Main Colors -->
    <Color x:Key="MainColor">#FF595959</Color>

    <SolidColorBrush x:Key="BackgroundBrush" Color="#1A1A1D" />

    <Style TargetType="{x:Type Button}" x:Key="WindowChromeButton">
        <Setter Property="Background" Value="Black" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF4B4747" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="WindowChromeCloseButton" BasedOn="{StaticResource WindowChromeButton}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="Red" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Windows -->
    <Style TargetType="{x:Type Window}" x:Key="WindowChromeStyle">
        <Setter Property="MinHeight" Value="400" />
        <Setter Property="MinWidth" Value="600" />
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome CornerRadius="0" GlassFrameThickness="0" CaptionHeight="30" />
            </Setter.Value>
        </Setter>
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="AllowsTransparency" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Grid Background="#1A1A1D" Margin="6">
                        <Border x:Name="ContainerBorder" Background="{StaticResource BackgroundBrush}" BorderBrush="Black" BorderThickness="1" IsHitTestVisible="True">
                            <DockPanel>
                                <DockPanel x:Name="TitleBar" DockPanel.Dock="Top" VerticalAlignment="Top" WindowChrome.IsHitTestVisibleInChrome="False"
                                           Height="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(WindowChrome.WindowChrome).ResizeBorderThickness}">
                                    <Button Content="r" Style="{StaticResource WindowChromeCloseButton}" Width="30" Height="25" x:Name="CloseButton" DockPanel.Dock="Right" HorizontalAlignment="Center" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Close" Click="CloseButtonClickCommand" />
                                    <Button Content="1" Style="{StaticResource WindowChromeButton}" Width="30" Height="25" x:Name="MaximizeButton" DockPanel.Dock="Right" HorizontalAlignment="Center" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True" Click="MaximazeButtonClickCommand" />
                                    <Button Content="0" Style="{StaticResource WindowChromeButton}" Width="30" Height="25" x:Name="MinimizeButton" DockPanel.Dock="Right" HorizontalAlignment="Center" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Minimize" Click="MinimizeButtonClickCommand" />
                                    <TextBlock VerticalAlignment="Center" Text="{TemplateBinding Title}" Margin="10,8" FontSize="14" Foreground="White" />
                                </DockPanel>
                            </DockPanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>